

// interface - implementações
// class - que vai derivar um objeto
// type - um esqueleto de como deve ser o objeto

type User = {
    uuid?: string;
    username: string;
    password?: string;
}

export default User;